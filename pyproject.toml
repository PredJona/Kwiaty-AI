[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "kwiaty"
version = "0.1.0"
description = "Asistente local-first modular para CachyOS"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
kwiaty = "kwiaty.cli.main:main"
